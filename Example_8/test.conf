# the checks to perform:

# format is:
# check = filename : variable1(precision) variable2(precision) ...
#
# (default precision is 1e-6)
#
# If you omit any vaiables, then it will just check
# if the file has been generated.
#
# You can define more than one check
#
# examples:

inputfile = quantum_poisson.tib
outputdir = output

check = dd_poisson_schroedinger_msh.dat : Ec eDensity
check = quantum_el_poisson_schroedinger.dat : EigenEnergy Occupation
check = quantum_el_poisson_schroedinger_msh.dat : eDensity
